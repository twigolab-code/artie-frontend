{
  "name": "georush",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "GeoRush — clone in stile Geometry Dash (Vite + Canvas 2D, JS vanilla)",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
