# Backend telemetria Artie/GeoRush — Lambda Function URL (eu-central-1).
# Vite espone solo le variabili con prefisso VITE_ (inlined a build time).
# Su Cloudflare Pages impostarle nelle build env (Production + Preview).
# Senza ENTRAMBE le variabili la telemetria è completamente disattivata (no-op).
#
# Per lo sviluppo locale: copiare questo file in .env.local (gitignorato via *.local).
VITE_ARTIE_SESSION_URL=https://6yjsozjqy3aymamyv6cdlep4hy0omcvo.lambda-url.eu-central-1.on.aws/
VITE_ARTIE_INGEST_URL=https://otnnhlvueubwb4hzwhnk364eha0perrz.lambda-url.eu-central-1.on.aws/
